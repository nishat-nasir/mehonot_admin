import 'package:auto_route/auto_route.dart';
import '../../template/template.dart';

@RoutePage()
class Menu01Page extends StatelessWidget {
  const Menu01Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
